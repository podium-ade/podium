version: "2"

linters:
  enable:
    - errcheck
    - govet
    - revive
    - staticcheck
  exclusions:
    presets:
      - std-error-handling

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/alvaroibarguen/podium
