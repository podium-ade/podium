{
  "name": "podium-agent-runtime",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "One turn of the Claude Agent SDK inside a Podium task container.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:images": "vitest run --config vitest.images.config.ts"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.3.259",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
