/// <reference types="vite/client" />

/** Stamped by Vite from the Makefile's VERSION; the UI is embedded in podium-server, so this is
 *  the version of the binary serving it. */
declare const __PODIUM_VERSION__: string;
