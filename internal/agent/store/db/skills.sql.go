// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: skills.sql

package agentdb

import (
	"context"
	"time"
)

const deleteStoredSkill = `-- name: DeleteStoredSkill :execrows
delete from skills where name = $1
`

func (q *Queries) DeleteStoredSkill(ctx context.Context, name string) (int64, error) {
	result, err := q.db.Exec(ctx, deleteStoredSkill, name)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const insertStoredSkill = `-- name: InsertStoredSkill :execrows

insert into skills (name, definition, updated_at, updated_by)
values ($1, $2, $3, $4)
on conflict (name) do nothing
`

type InsertStoredSkillParams struct {
	Name       string
	Definition []byte
	UpdatedAt  time.Time
	UpdatedBy  string
}

// Insert and update are separate statements rather than one upsert so that "create" and
// "replace" cannot be confused: the row count says whether the name was already taken, and
// whether it was there to replace, without a read before the write.
func (q *Queries) InsertStoredSkill(ctx context.Context, arg InsertStoredSkillParams) (int64, error) {
	result, err := q.db.Exec(ctx, insertStoredSkill,
		arg.Name,
		arg.Definition,
		arg.UpdatedAt,
		arg.UpdatedBy,
	)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const listStoredSkills = `-- name: ListStoredSkills :many
select name, definition, updated_at, updated_by from skills order by name
`

func (q *Queries) ListStoredSkills(ctx context.Context) ([]Skill, error) {
	rows, err := q.db.Query(ctx, listStoredSkills)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Skill{}
	for rows.Next() {
		var i Skill
		if err := rows.Scan(
			&i.Name,
			&i.Definition,
			&i.UpdatedAt,
			&i.UpdatedBy,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateStoredSkill = `-- name: UpdateStoredSkill :execrows
update skills
set definition = $1, updated_at = $2, updated_by = $3
where name = $4
`

type UpdateStoredSkillParams struct {
	Definition []byte
	UpdatedAt  time.Time
	UpdatedBy  string
	Name       string
}

func (q *Queries) UpdateStoredSkill(ctx context.Context, arg UpdateStoredSkillParams) (int64, error) {
	result, err := q.db.Exec(ctx, updateStoredSkill,
		arg.Definition,
		arg.UpdatedAt,
		arg.UpdatedBy,
		arg.Name,
	)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}
